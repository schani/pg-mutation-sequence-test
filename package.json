{
  "name": "pg-mutation-sequence-test",
  "version": "1.0.0",
  "description": "Test demonstrating PostgreSQL sequence locking for mutation ordering",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/pg": "^8.10.9",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "dependencies": {
    "pg": "^8.11.3"
  }
}
